import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export const GlobalStyleSheet = StyleSheet.create({
    text: {
      color: 'black',
      fontSize: 16,
      // Other common text styles can be added here
    },
  });